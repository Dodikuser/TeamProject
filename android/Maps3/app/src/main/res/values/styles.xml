<resources>

    <!-- Виджет темы -->
    <style name="Widget.Theme.Maps1.MyView" parent="">
        <item name="android:background">@color/gray_400</item>
        <item name="exampleColor">@color/light_blue_400</item>
        <item name="android:fontFamily">@font/roboto_regular</item>
    </style>

    <!-- Кастомный круглый ImageView -->
    <style name="CustomCircleImageView" parent="ShapeAppearance.MaterialComponents.SmallComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item> <!-- Круглая форма -->
    </style>

    <!-- Карточка результата поиска -->
    <style name="SearchResultCard.TextAppearance" parent="TextAppearance.AppCompat.Small">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">16sp</item>
        <item name="android:maxLines">2</item>
        <item name="android:ellipsize">end</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">4dp</item>
    </style>

    <!-- Внешний вид рейтинга -->
    <style name="RatingTextAppearance" parent="TextAppearance.AppCompat.Small">
        <item name="android:textColor">@color/gold</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!-- Иконка-кнопка -->
    <style name="IconButton" parent="Widget.AppCompat.ImageButton">
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>
        <item name="tint">@color/primary</item>
    </style>

    <!-- Залитая кнопка -->
    <style name="FilledButton" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/primary</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="cornerRadius">8dp</item>
    </style>

    <!-- Обведённая кнопка -->
    <style name="OutlinedButton" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="strokeColor">@color/primary</item>
        <item name="android:textColor">@color/primary</item>
        <item name="cornerRadius">8dp</item>
    </style>

    <!-- Базовая тема приложения -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="ratingBarStyle">@style/CustomRatingBar</item>
        <item name="ratingBarStyleSmall">@style/CustomRatingBar.Small</item>
    </style>

    <!-- Текстовая кнопка -->
    <style name="TextButton" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@color/primary</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!-- RatingBar -->
    <style name="RatingBar" parent="Widget.AppCompat.RatingBar">
        <item name="android:minHeight">24dp</item>
        <item name="android:maxHeight">24dp</item>
    </style>

    <!-- Закруглённый TextInputLayout -->
    <style name="ShapeAppearanceOverlay.TextInputLayout" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">8dp</item>
    </style>

    <!-- Кастомный RatingBar -->
    <style name="CustomRatingBar" parent="Widget.AppCompat.RatingBar">
        <item name="colorControlNormal">@color/primary_light</item>
        <item name="colorControlActivated">@color/primary</item>
        <item name="android:minHeight">24dp</item>
        <item name="android:maxHeight">24dp</item>
    </style>

    <!-- Маленький кастомный RatingBar -->
    <style name="CustomRatingBar.Small" parent="Widget.AppCompat.RatingBar.Small">
        <item name="colorControlNormal">@color/primary_light</item>
        <item name="colorControlActivated">@color/primary</item>
        <item name="android:minHeight">16dp</item>
        <item name="android:maxHeight">16dp</item>
    </style>

</resources>
