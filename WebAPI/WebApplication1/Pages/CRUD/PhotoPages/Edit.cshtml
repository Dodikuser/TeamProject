@page
@model AdminPanel.Pages.CRUD.PhotoPages.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Photo</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Photo.Id" />
            <div class="form-group">
                <label asp-for="Photo.Path" class="control-label"></label>
                <input asp-for="Photo.Path" class="form-control" />
                <span asp-validation-for="Photo.Path" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Photo.PlaceId" class="control-label"></label>
                <select asp-for="Photo.PlaceId" class="form-control" asp-items="ViewBag.PlaceId"></select>
                <span asp-validation-for="Photo.PlaceId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
