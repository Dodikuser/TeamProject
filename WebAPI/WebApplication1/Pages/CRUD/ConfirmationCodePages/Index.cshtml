@page
@model AdminPanel.Pages.CRUD.ConfirmationCodePages.IndexModel

@{
    ViewData["Title"] = "ConfirmationCode";
}

<h1>ConfirmationCode</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ConfirmationCode[0].Code)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConfirmationCode[0].CreatedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConfirmationCode[0].ExpiresAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConfirmationCode[0].IsUsed)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConfirmationCode[0].User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConfirmationCode[0].Place)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ConfirmationCode) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExpiresAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsUsed)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Place.Address)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
